% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Functions.R
\name{qc_plot}
\alias{qc_plot}
\title{Generate and Save QC Violin Plots with Optional Cutoff Lines}
\usage{
qc_plot(
  seurat_obj,
  features,
  output_path,
  show_cutoff = FALSE,
  x_var = orig.ident,
  fill_var = "",
  cutoff_val = ""
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing the single-cell RNA-seq data.}

\item{features}{A character vector of feature names to be plotted.}

\item{output_path}{A string specifying the directory where the output images
should be saved. If the directory does not exist, it will be created.}

\item{show_cutoff}{A logical value indicating whether to display cutoff values
as horizontal error bars (default: FALSE).}

\item{x_var}{A string specifying the variable to be used for the x-axis (e.g., sample identity).}

\item{fill_var}{A string specifying the variable to be used for the fill color.
If empty (`''`), it defaults to `x_var`.}

\item{cutoff_val}{A numeric vector specifying the cutoff values corresponding
to each feature. If empty, no cutoffs are applied.}
}
\value{
A named list of ggplot objects corresponding to the generated violin plots.
}
\description{
This function generates violin plots for specified features in a Seurat object,
optionally including cutoff values as horizontal lines, and saves them as PNG images.
The plots are arranged in grids of 2x2 per image for efficient visualization.
}
\examples{
\dontrun{
qc_plot(seurat_obj, features = c("nFeature_RNA", "percent.mt"),
        output_path = "qc_plots", show_cutoff = TRUE,
        x_var = "orig.ident", fill_var = "sample_group",
        cutoff_val = c(1000, 5))
}
}
